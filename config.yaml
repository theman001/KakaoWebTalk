# KakaoWebTalk Configuration
server:
  port: 80
  mode: "production"

kakao:
  appVersion: "3.4.2"
  protocolVersion: "1.1"
  # deviceUuid: "your-device-uuid"

database:
  user: "ADMIN"
  password: "KakaoWebDB0149"
  # Wallet의 tnsnames.ora에서 확인한 서비스 이름으로 수정 (예: kakaowebdb_high)
  connectString: "kakaowebdb_high"
  walletPath: "/KakaoWebTalk/server/lib/wallet"
  walletLocation: config.database.walletPath
